# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'Noah' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Noah
  #  pod 'Parse'
  #  pod 'Bolts-Swift', :git => 'https://github.com/BoltsFramework/Bolts-Swift'
  #  pod 'ParseLiveQuery'
  #  pod 'ParseLiveQuery', :git => 'https://github.com/parse-community/ParseLiveQuery-iOS-OSX', :branch => 'release-2.4.0'
  #  pod 'Parse', '~> 1.17.0-alpha.3'

end

#post_install do | installer |
#    installer.pods_project.targets.each do |target|
#        if target.name.end_with? 'Bolts-Swift'
#            target.build_configurations.each do |config|
#                config.build_settings['SWIFT_VERSION'] = '3.0'
#            end
#        end
#    end
#$end

